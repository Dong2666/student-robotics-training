# Daily Report — 2026-09-19

## Today's Goal

Close Week 2 Milestone 2.1 and start Milestone 2.2 (ROS 2 environment).

## Completed

- M2.1 (Finish and Release Week 1):
  - Created `chore/week1-release` branch and added `.gitignore`
    (virtual environments, Python caches, logs)
  - Verified `main` from a fresh clone in a new directory: created a
    new venv, installed dependencies, and ran every documented command
    (both scripts, plus the missing-argument error case) — all passed
    on the first attempt
  - Recorded the verification steps in
    `docs/clean_clone_verification.md`
  - PR #4 opened and merged
- M2.2: installed Ubuntu 22.04 (WSL2) on the D drive
- Ubuntu first-launch user creation in progress

## Problems Encountered

- M2.1: none — every documented command passed on the first attempt in
  the clean clone.

## Claude Code Usage

- Used it to run the M2.1 clean-clone verification (fresh clone, new
  venv, dependency install, script runs) and to draft the verification
  record; I reviewed the record before committing.
- Used it for the M2.2 disk checks and the Ubuntu install; I performed
  the interactive user setup.

## What I Learned

- Some install steps are interactive and cannot be automated.

## Plan for Next Session

Install ROS 2 Humble, run the official talker/listener verification,
create `robot_ws`, and build it with colcon. talker/listener is NOT
yet run — verification pending.
